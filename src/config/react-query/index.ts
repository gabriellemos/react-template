import { QueryClient } from 'react-query'

// Check API on how to configure cache properties
// https://react-query.tanstack.com/reference/QueryClient
const queryClient = new QueryClient()

export default queryClient
